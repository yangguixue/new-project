<!--pages/components/noLogin/index.wxml-->
<view class='box' wx:if="{{isHide}}">
  <view class='content'>
    <button loading="{{isLoading}}" class='ok' bind:getuserinfo="handleGetUserInfo" open-type='getUserInfo'>微信一键登录</button>
  </view> 
  <view class='block' bindtap="hide" />
</view>

