<!--pages/container/discountList/index.wxml-->
<block wx:for="{{discount}}" wx:key="{{index}}">
  <discount items="{{item}}" isDetail="{{false}}" />
</block> 

<loading isLoading="{{isLoading}}" list="{{discount}}" />
