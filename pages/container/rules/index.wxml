<!--pages/container/rules/index.wxml-->
<view class='box'>
  <view class='content'>
    <rich-text nodes="{{nodes}}"></rich-text>  
  </view>
  <button class='button' wx:if="{{name == 'bzj' && is_owner}}">
    {{hasPay == 'true' ? '退回保证金' : '缴纳保证金'}}
  </button>
</view>
